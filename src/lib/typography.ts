/** Canonical typography class strings — see DESIGN.md §4 Typography. */

/** Section header that titles a content block or settings group. Bagel Fat One display. */
export const sectionHeadingClass = 'font-display text-2xl text-text'

/** Eyebrow / form-field label — the 10px caps label style. */
export const fieldLabelClass = 'text-xs uppercase tracking-label text-text-3'
